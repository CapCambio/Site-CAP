Prompt para Ajustar Calculadora de Moedas - Diretrizes Exatas
1. INICIALIZAÇÃO DA CALCULADORA (Estado Zerado)
PROBLEMA: A calculadora não inicia zerada
SOLUÇÃO EXATA:

No estado inicial do componente, definir amount como string vazia: useState<string>("")
No campo de input, definir placeholder: placeholder="Digite o valor"
No resultado convertido, mostrar: {convertedAmount || "0,00"} (sempre mostra 0,00 quando vazio)
Nunca inicializar amount com nenhum valor numérico
2. CENTRALIZAÇÃO DOS BOTÕES "PRECISO DE/TENHO"
PROBLEMA: Botões "Preciso de/Tenho" não estão centralizados
SOLUÇÃO EXATA:

Container dos botões:

<div className="flex items-center justify-center mb-2 sm:mb-3">
Estrutura completa dos botões:

<div className="flex items-center justify-center mb-2 sm:mb-3">
  <button
    onClick={() => setMode("preciso")}
    className={`px-3 py-1 rounded-l-lg text-sm font-medium transition-all duration-150 ${
      mode === "preciso" 
        ? "bg-[#f3b234] text-[#1a1a1a] shadow-[inset_0_2px_4px_rgba(0,0,0,0.2)] border border-[#e6a429]" 
        : "bg-white text-black hover:bg-gray-100 shadow-[0_2px_4px_rgba(0,0,0,0.1)] border border-gray-200"
    }`}
  >
    Preciso de
  </button>
  <button
    onClick={() => setMode("tenho")}
    className={`px-3 py-1 rounded-r-lg text-sm font-medium transition-all duration-150 ${
      mode === "tenho" 
        ? "bg-[#f3b234] text-[#1a1a1a] shadow-[inset_0_2px_4px_rgba(0,0,0,0.2)] border border-[#e6a429]" 
        : "bg-white text-black hover:bg-gray-100 shadow-[0_2px_4px_rgba(0,0,0,0.1)] border border-gray-200"
      }`}
  >
    Tenho
  </button>
</div>
Posicionamento: Os botões devem estar ACIMA dos campos de conversão, logo abaixo do título "Conversor de Moedas"

3. ESTRUTURA COMPLETA DO LAYOUT
Ordem dos elementos (de cima para baixo):

Título "Conversor de Moedas" (centralizado)
Botões "Preciso de/Tenho" (centralizados com justify-center)
Campos de conversão (input + resultado)
Container principal:

<div className="currency-converter relative max-w-3xl mx-auto mt-1 mb-4">
  <div className="bg-[#252525] p-3 pb-8 sm:p-6 sm:pb-12 rounded-xl">
4. CORES E ESTILOS DOS BOTÕES
Botão ativo (selecionado):

Background: bg-[#f3b234] (amarelo dourado)
Texto: text-[#1a1a1a] (preto escuro)
Sombra: shadow-[inset_0_2px_4px_rgba(0,0,0,0.2)]
Borda: border border-[#e6a429]
Botão inativo:

Background: bg-white
Texto: text-black
Hover: hover:bg-gray-100
Sombra: shadow-[0_2px_4px_rgba(0,0,0,0.1)]
Borda: border border-gray-200
5. RESPONSIVIDADE DOS BOTÕES
Padding: px-3 py-1 (igual para ambos)
Tamanho fonte: text-sm
Font weight: font-medium
Transição: transition-all duration-150
Border radius: rounded-l-lg (esquerdo) e rounded-r-lg (direito)
6. INICIALIZAÇÃO DO ESTADO
const [mode, setMode] = useState<"tenho" | "preciso">("preciso");
const [amount, setAmount] = useState<string>("");
const [convertedAmount, setConvertedAmount] = useState<string>("");
7. VALIDAÇÃO FINAL
Após implementar, verificar:

 Campo de input inicia vazio (placeholder visível)
 Resultado mostra "0,00" inicialmente
 Botões estão perfeitamente centralizados
 Botão "Preciso de" inicia selecionado (amarelo)
 Layout: Título → Botões → Campos de conversão
 Responsividade funciona em mobile e desktop
IMPORTANTE: Use exatamente essas classes CSS e estrutura JSX. Não modifique os valores de padding, margin ou cores especificados.